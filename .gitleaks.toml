title = "rinblog gitleaks 配置"

[extend]
useDefault = true

[[allowlists]]
description = "允许文档中的占位示例"
regexes = [
  '''(?i)your[-_ ]?(secret|token|password|key)''',
  '''(?i)<(secret|token|password|key)>''',
  '''(?i)example[-_ ]?(secret|token|password|key)'''
]
paths = [
  '''docs/.*''',
  '''\.pre-commit-config\.yaml'''
]
